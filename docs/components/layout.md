# Layout 布局

栅格或结构化布局能力，适合页面分区和响应式排版。

## 当前状态

该组件已经接入统一导出，可通过 `@qingyu6688/my-ui` 引入使用。

## 基础用法

```vue
<template>
  <my-layout>Layout</my-layout>
</template>
```

## 设计说明

- 分类：基础组件。
- 布局组件需要保持稳定尺寸和响应式断点，不应干扰业务内容语义。
- 后续会继续补齐 Props、Events、Slots 和 Exposes 表格。
